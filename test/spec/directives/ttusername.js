'use strict';

describe('Directive: ttUsername', function () {

  // load the directive's module
  beforeEach(module('testTeachApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
